server:
  port: 8083
feign:
  okhttp:
    enabled: true
hm:
  db:
    host: ${hm.db.host}
    database: ${hm.db.database}
    pw: ${hm.db.pw}
  swagger:
    title: ${hm.swagger.title}
    package: ${hm.swagger.package}